# Pinned to versions used in development. Loosen the lower bounds if needed,
# but ortools and FastAPI both ship breaking changes occasionally — pinning
# avoids surprises on a fresh `pip install`.

ortools>=9.10
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1
python-multipart>=0.0.9   # FastAPI form parsing
httpx>=0.27               # only needed for the test suite (TestClient)
pytest>=8.0               # only needed for the test suite
