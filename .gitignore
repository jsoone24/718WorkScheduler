__pycache__/
*.pyc

# Transient runtime data — regenerated by user via the web UI.
# `data/users.json` IS tracked (the platoon roster), but the rest is local state.
data/vacations.json
data/outings.json
data/strikeforce.json
data/schedules.json
data/ledger.json
data/settings.json
data/*.json.tmp
